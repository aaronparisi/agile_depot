<%# logger.debug "@current_item.id = #{@current_item.id}" if @current_item%>
<%# logger.debug "line_item.id = #{line_item.id}"%>
<% if @current_item && @current_item.id == line_item.id %>
  <div class="single_line_item" id="current_item">
<% else %>
  <div class="single_line_item">
<% end %>

  <div class="line_item_quantity">
    <%= line_item.quantity %> &times
  </div>

  <div class="line_item_title">
    <%= line_item.product.title %>
  </div>

  <div class="line_item_price">
    <%= number_to_currency(line_item.total_price) %>
  </div>

  <div class="line_item_inc_button">
    <%= button_to "inc", line_items_path(product_id: line_item.product_id, dir: "inc"), remote: true %>
  </div>

  <div class="line_item_dec_button">
    
    <% unless line_item.quantity == 1 %>
      <%= button_to "dec", line_items_path(product_id: line_item.product_id, dir: "dec"), remote: true %>
    <% end %>
  </div>
  
  <div class="line_item_destroy_button">
    <%# link_to raw('<i class="icon-trash icon-white"></i>'), line_item, method: :delete, data: {confirm: "Remove \'#{line_item.product.title}' from cart?"}, :class => 'btn btn-danger' %>
    <%= button_to "rem", line_item, method: :delete, data: {confirm: "Remove \'#{line_item.product.title}' from cart?"}, remote: true %>
  </div>

</div>